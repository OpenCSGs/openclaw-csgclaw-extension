interface:
  display_name: "Conventional Commit"
  short_description: "Draft commit messages from current git changes"
  default_prompt: "Use $conventional-commit to generate Conventional Commit messages from the current repo changes. Never add Co-authored-by or any agent attribution. If I explicitly ask to commit, use the recommended message with apply_commit.py."
